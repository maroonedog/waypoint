{
  "name": "form-contract-workspace",
  "private": true,
  "type": "module",
  "workspaces": ["packages/*"],
  "scripts": {
    "build": "npm run build -w form-contract && npm run build -w form-contract-resolver-zod",
    "test": "node --test test/*.test.mjs",
    "test:types": "tsc -p test/types/tsconfig.json",
    "verify": "npm run build && npm run test && npm run test:types"
  },
  "devDependencies": {
    "typescript": "^5.9.0",
    "zod": "^4.0.14"
  }
}
