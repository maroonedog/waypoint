// Layer 1 — AutoForm, and every rung of the widget table it draws through.
// See ../vite-example.ts.
import { exampleConfig } from "../vite-example.js";

export default exampleConfig({ port: 5186 });
