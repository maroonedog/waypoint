// A form drawn across several screens, run against the package's source.
// See ../vite-example.ts.
import { exampleConfig } from "../vite-example.js";

export default exampleConfig({ port: 5184 });
