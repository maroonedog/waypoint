{
  "//": "The `./resolver-zod` entry. It names zod in a type position only, and since the descriptors now come from zod's own JSON Schema it references `./resolver-standard` and keeps just the refinement — the two facts a JSON Schema document cannot carry about the schema that produced it.",
  "extends": "../../tsconfig.base.json",
  "compilerOptions": {
    "lib": ["ES2020", "DOM"],
    "rootDir": ".",
    "outDir": "../../dist/resolver-zod",
    "tsBuildInfoFile": "../../.tsbuildinfo/resolver-zod.tsbuildinfo"
  },
  "include": ["."],
  "references": [{ "path": "../contract" }, { "path": "../resolver-standard" }]
}
