{
  "//": "The `./react` entry: the only one that needs a JSX factory, and the only one whose built output names `react` at all. `../dom` widens to the DOM too, because a field's props are DOM attributes; what stays here is what React owns. Keeping both widenings out of `./core` is what its own `lib` line is for.",
  "extends": "../../tsconfig.base.json",
  "compilerOptions": {
    "lib": ["ES2020", "DOM"],
    "jsx": "react-jsx",
    "rootDir": ".",
    "outDir": "../../dist/react",
    "tsBuildInfoFile": "../../.tsbuildinfo/react.tsbuildinfo"
  },
  "include": ["."],
  "references": [
    { "path": "../contract" },
    { "path": "../core" },
    { "path": "../dom" }
  ]
}
